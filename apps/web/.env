# Không chứa bí mật — commit vào repo để Prisma CLI và Next.js cùng đọc được.
# Cần ghi đè gì thì tạo apps/web/.env.local (đã bị gitignore).

# Đường dẫn SQLite tính từ thư mục chứa schema (apps/web/prisma/).
DATABASE_URL="file:../../../data/app.db"

SPEECH_URL="http://127.0.0.1:8000"
OLLAMA_URL="http://127.0.0.1:11434"
